import React from 'react'
import { Header } from '../../../../payload/payload-types'

const MobileHeader = ({ header }: { header: Header }) => {
  return <div>MobileHeader</div>
}

export default MobileHeader
